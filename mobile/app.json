{
  "expo": {
    "name": "Incline",
    "slug": "incline",
    "scheme": "incline",
    "version": "1.0.0",
    "orientation": "portrait",
    "userInterfaceStyle": "light",
    "newArchEnabled": true,
    "platforms": [
      "ios",
      "android"
    ],
    "splash": {
      "backgroundColor": "#fbf7f0"
    },
    "ios": {
      "supportsTablet": true,
      "bundleIdentifier": "nz.ac.auckland.incline",
      "infoPlist": {
        "NSLocationWhenInUseUsageDescription": "Incline checks once, at the moment you start a session, to see whether you're at a verified study spot. It never tracks you in the background.",
        "//": "The dev backend is plain http on a LAN address. Expo Go already permits this; the exception only matters if this is ever built as a standalone app.",
        "NSAppTransportSecurity": {
          "NSAllowsLocalNetworking": true
        }
      }
    },
    "extra": {
      "//": "Leave apiBaseUrl unset in development: the app derives the backend host from the Expo dev server, so it follows the laptop across networks. Set it to point at a deployed backend."
    },
    "plugins": [
      "expo-dev-client",
      [
        "expo-sensors",
        {
          "motionPermission": "Allow Incline to detect when you plant your phone face-down for a focus session."
        }
      ],
      [
        "expo-build-properties",
        {
          "android": {
            "usesCleartextTraffic": true
          }
        }
      ]
    ],
    "android": {
      "package": "nz.ac.auckland.incline",
      "edgeToEdgeEnabled": true,
      "permissions": [
        "ACCESS_COARSE_LOCATION",
        "ACCESS_FINE_LOCATION",
        "SYSTEM_ALERT_WINDOW",
        "FOREGROUND_SERVICE",
        "FOREGROUND_SERVICE_SPECIAL_USE",
        "POST_NOTIFICATIONS"
      ]
    }
  }
}
